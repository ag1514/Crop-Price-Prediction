<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="min-h-screen w-full bg-zinc-900 p-10 text-white">
        <div class="flex justify-end">
            <a class="text-red-500" href="/logout">Logout</a>
        </div>
        <div class="flex gap-2">
            <div class="w-10 h-10 rounded-md overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/upload/<%= user.profilepic %>" alt="">
            </div>
            <h3 class="text-3xl"><span class="font-light">Hello</span>, <%= user.name %></h3>
        </div>
        <h5 class="text-blue-500 ">You can create a new post.</h5>
        <form action="/post" method="post">
            <textarea placeholder="What's on your mind ?" name="content" class="block mb-3 px-2 py-4 w-1/3 mt-5 resize-none bg-zinc-900 border-2 border-zinc-800 rounded-md outline-none"></textarea>
            <input class="block bg-purple-500 px-5 py-2 rounded-md" type="submit" value="Submit your post">
        </form>
        <div>
            <h5 class="text-zinc-500 mt-10">Your Posts.</h5>
            <div class="postcontainer">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-4 rounded-md bg-zinc-800 border-[1px] border-zinc-800 mt-2">
                        <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                        <p class="text-sm tracking-tight"><%= post.content%></p>
                        <small class="text-purple-500"> <%= post.likes.length%> likes</small>
                        <div class="btn flex gap-5 mt-1">
                            <a class="text-yellow-500" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike"%>
                            </a>
                            <a class="text-zinc-500" href="/edit/<%= post._id %>">edit</a>
                        </div>
                        <div class="flex justify-end">
                            <a class="text-red-500" href="/delete/<%= post._id %>">delete</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>
